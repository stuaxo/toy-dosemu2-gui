project('subprocess', 'c',
  version : '2.0.0',
  license : 'Unlicense'
)

subprocess_inc = include_directories('.')

# This allows other parts of the build to get the dependency
subprocess_dep = declare_dependency(
  include_directories : subprocess_inc
)
